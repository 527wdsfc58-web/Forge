Forge V0.20
GitHub Pages deployment package.
Keep index.html at the root of the selected Pages source.

V0.20 is a UX cleanup release. On Today, View workouts is now a compact + button and AI workout is now a compact ✨ button. Existing workout, random workout, AI generator and tracking functionality is unchanged.

The AI Workout Generator UI remains available but the secure endpoint is not required for normal Forge functionality. The browser never contains an OpenAI API key.
